import { ProductCard } from "../../components/product-card";
import { api } from "../../lib/api";
type Product = { id: string; name: string; slug: string; price: number; compareAtPrice?: number | null; images: string };
export default async function Shop() { let products: Product[] = []; try { products = await api<Product[]>("/products"); } catch {} return <main className="shell py-12"><p className="text-sm font-bold uppercase text-leaf">The collection</p><h1 className="mt-2 text-4xl font-black">Shop idzen</h1><div className="my-8 flex flex-wrap gap-2"><button className="button secondary">All</button><button className="button secondary">Home</button><button className="button secondary">Wellbeing</button><button className="button secondary">Gifts</button></div>{products.length ? <div className="grid grid-cols-2 gap-x-5 gap-y-10 md:grid-cols-4">{products.map(product => <ProductCard key={product.id} product={product} />)}</div> : <p className="py-14 text-[#65726b]">No products have been published yet.</p>}</main> }
