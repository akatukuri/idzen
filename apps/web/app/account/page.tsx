"use client";
import { FormEvent, useState } from "react";
import { api } from "../../lib/api";
export default function Account() { const [message, setMessage] = useState(""); async function login(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); try { const result = await api<{ token: string }>("/auth/login", { method: "POST", body: JSON.stringify({ email: data.get("email"), password: data.get("password") }) }); localStorage.setItem("idzen_token", result.token); setMessage("Welcome back."); } catch (error) { setMessage(error instanceof Error ? error.message : "Unable to sign in"); } } return <main className="shell grid max-w-xl py-16"><p className="text-sm font-bold uppercase text-leaf">Your idzen</p><h1 className="mt-2 text-4xl font-black">Sign in</h1><form onSubmit={login} className="mt-8 grid gap-4"><input className="field" name="email" type="email" placeholder="Email address" required /><input className="field" name="password" type="password" placeholder="Password" required minLength={8} /><button className="button" type="submit">Sign in</button>{message && <p className="text-sm">{message}</p>}</form></main> }
