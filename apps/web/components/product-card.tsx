import Link from "next/link";
type Product = { id: string; name: string; slug: string; price: number; compareAtPrice?: number | null; images: string };
export function ProductCard({ product }: { product: Product }) { let image: string | undefined; try { image = JSON.parse(product.images)[0]; } catch {} return <Link href={`/shop/${product.slug}`} className="group block"><div className="aspect-[4/5] overflow-hidden bg-[#e6ebe6]">{image ? <img className="h-full w-full object-cover transition group-hover:scale-105" src={image} alt={product.name} /> : <div className="flex h-full items-center justify-center text-sm text-[#65726b]">idzen</div>}</div><div className="mt-3 flex justify-between gap-3"><div><h2 className="font-bold">{product.name}</h2><p className="mt-1 text-sm">Rs. {(product.price / 100).toLocaleString("en-IN")}</p></div>{product.compareAtPrice && <span className="text-sm text-[#65726b] line-through">Rs. {(product.compareAtPrice / 100).toLocaleString("en-IN")}</span>}</div></Link> }
